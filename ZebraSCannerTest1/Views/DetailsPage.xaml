<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:ZebraSCannerTest1.ViewModels"
             x:Class="ZebraSCannerTest1.Views.DetailsPage"
             x:DataType="viewmodel:DetailsViewModel"
             Title="Details">

    <VerticalStackLayout Padding="20">
        <Label Text="BARCODE" TextColor="Black"/>
        <Label Text="{Binding ProductBarcode}" TextColor="black" FontSize="20" Margin="0,0,0,30"/>

        <Label Text="?????????????" TextColor="black"/>
        <Entry Text="{Binding ScannedQuantity, Mode=TwoWay}"
               Keyboard="Numeric" FontSize="20" TextColor="black" Margin="0,0,0,8"/>

        <Label Text="?????????? ?????????" TextColor="black"/>
        <Label Text="{Binding InitialQuantity}" TextColor="black" FontSize="20" Margin="0,0,0,8"/>

        <Button Text="???????" Command="{Binding SaveCommand}"/>
    </VerticalStackLayout>
</ContentPage>