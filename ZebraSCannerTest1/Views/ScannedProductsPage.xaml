<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:ZebraSCannerTest1.ViewModels"
             xmlns:models="clr-namespace:ZebraSCannerTest1.Models"
             x:Class="ZebraSCannerTest1.Views.ScannedProductsPage"
             x:DataType="viewmodel:ScannedProductsViewModel"
             Title="Scanned Products">

    <CollectionView ItemsSource="{Binding ScannedProductsStats}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:StatsProduct">
                <Grid Padding="8" ColumnDefinitions="2*,*,*,*">
                    <Label TextColor="black" Text="{Binding Barcode}" />
                    <Label Grid.Column="1" TextColor="black" Text="{Binding ScannedQuantity}" />
                    <Label Grid.Column="2" TextColor="black" Text="{Binding InitialQuantity}"/>
                    <Label Grid.Column="2" TextColor="black" Text="{Binding Difference}" HorizontalTextAlignment="End"/>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
